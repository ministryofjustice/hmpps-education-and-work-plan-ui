/**
 * Enum of search page 'sort by' columns. Values aligned with the UI.
 */
enum SortBy {
  NAME = 'name',
  RELEASE_DATE = 'release-date',
  RECEPTION_DATE = 'reception-date',
  LOCATION = 'location',
  STATUS = 'status',
}

export default SortBy
